{
    "base_url": "http://localhost:9000",
    "name": "Api Doc",
    "description": "Host API documentation for REST services, and facilities design of resource first APIs.",
    "resources": {

        "product": {
            "description": "A product is a top level object.",
            "fields": [
                { "name": "guid", "type": "string", "description": "Internal unique identifier for this product." },
                { "name": "name", "type": "string" },
            ],
            "operations": [
                {
                    "method": "GET",
                    "description": "Search all users. Results are always paginated. You must specify at least 1 parameter - either a guid, email or token.",
                    "parameters": [
                        { "name": "guid", "type": "string", "description": "Product guid.", "required": false },
                    ],
                    "responses": [
                        { "code": 200, "result": ["product"] }
                    ]
                },
            ]
        },
    }
}
